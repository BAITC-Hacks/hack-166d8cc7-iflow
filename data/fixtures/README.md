# Fixtures

raw-sha256.json records the original archive bytes (including starter-kit documentation). Tests build small synthetic fixtures in temporary directories; never edit data/raw. Original archive: career_quest_dataset.zip, case_1/career_quest_dataset/.
